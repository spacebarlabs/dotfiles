name: Dotfiles Vim Tests

on: [push, pull_request]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Install Vim
        run: sudo apt-get update && sudo apt-get install -y vim

      - name: Run Vim Tests
        # -S points to the runner inside .vim/tests/
        run: vim -Es -u NONE -S .vim/tests/test_runner.vim
